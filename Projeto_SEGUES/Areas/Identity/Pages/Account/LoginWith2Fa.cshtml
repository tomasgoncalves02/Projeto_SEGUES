@page
@model LoginWith2FaModel

@{
    ViewData["Title"] = "Autenticação de dois fatores";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>O seu login está protegido com uma aplicação de autenticação. Introduza o seu código de autenticação abaixo.</p>
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="All" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                <label asp-for="Input.TwoFactorCode" class="form-label"></label>
                <span asp-validation-for="Input.TwoFactorCode" class="text-danger small"></span>
            </div>
            <div class="checkbox mb-3">
                <label asp-for="Input.RememberMachine" class="form-label">
                    <input asp-for="Input.RememberMachine" />
                    @Html.DisplayNameFor(m => m.Input.RememberMachine)
                </label>
            </div>
            <div>
                <button type="submit" class="w-100 btn btn-lg btn-primary">Entrar</button>
            </div>
        </form>
    </div>
</div>
<p>
    Não tem acesso ao seu dispositivo de autenticação? Pode 
    <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">fazer login com um código de recuperação</a>.
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}