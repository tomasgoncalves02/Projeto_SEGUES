@model Projeto_SEGUES.Models.User

@{
    ViewData["Title"] = "Apagar Utilizador";
}

<div class="container mt-4">
    <h1>Apagar Utilizador</h1>
    <hr />

    <h3>Tens a certeza que queres apagar este utilizador?</h3>
    <p class="text-muted">Esta ação não pode ser revertida.</p>

    <div class="card mt-4">
        <div class="card-header">
            Utilizador a remover
        </div>
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-2">Nome</dt>
                <dd class="col-sm-10">@Model.FirstName @Model.LastName</dd>

                <dt class="col-sm-2">Email</dt>
                <dd class="col-sm-10">@Model.Email</dd>

                <dt class="col-sm-2">Saldo Atual</dt>
                <dd class="col-sm-10">@Model.Balance.ToString("C")</dd>
            </dl>
        </div>
        <div class="card-footer bg-white">
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />

              
                <button type="submit" class="btn btn-primary">Sim, Apagar Definitivamente</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
            </form>
        </div>
    </div>
</div>