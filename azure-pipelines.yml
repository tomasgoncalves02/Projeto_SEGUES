trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  webAppName: 'projeto-segues-app-linux'
  azureServiceConnection: 'ConexaoAzure'

stages:

# ─────────────────────────────────────────
# STAGE 1 — BUILD
# ─────────────────────────────────────────
- stage: Build
  displayName: 'CI - Compilação e Artefactos'
  jobs:
  - job: BuildJob
    displayName: 'Compilar projeto .NET'
    steps:
    - task: UseDotNet@2
      displayName: 'Instalar .NET SDK 8.x'
      inputs:
        version: '8.x'

    - script: dotnet publish --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)
      displayName: 'Dotnet Publish'

    - task: ArchiveFiles@2
      displayName: 'Gerar Pacote ZIP'
      inputs:
        rootFolderOrFile: '$(Build.ArtifactStagingDirectory)'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'

    - publish: $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip
      artifact: drop
      displayName: 'Publicar Artefacto'

# ─────────────────────────────────────────
# STAGE 2 — TESTES (Versão Estável)
# ─────────────────────────────────────────
- stage: Test
  displayName: 'CI - Testes Automatizados'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - job: TestJob
    displayName: 'Executar testes xUnit'
    steps:
    - task: UseDotNet@2
      displayName: 'Instalar .NET SDK 8.x'
      inputs:
        version: '8.x'

    - script: >
        dotnet test
        --configuration $(buildConfiguration)
        --logger "trx;LogFileName=TestResults.trx"
        --results-directory $(Agent.TempDirectory) || true
      displayName: 'Correr testes unitários'

    - task: PublishTestResults@2
      displayName: 'Publicar resultados no Azure'
      condition: succeededOrFailed()
      inputs:
        testResultsFormat: 'VSTest'
        testResultsFiles: '$(Agent.TempDirectory)/**/*.trx'
        failTaskOnFailedTests: false

# ─────────────────────────────────────────
# STAGE 3 — QUALIDADE
# ─────────────────────────────────────────
- stage: Quality
  displayName: 'CI - Análise de Qualidade'
  dependsOn: Test
  condition: succeeded()
  jobs:
  - job: QualityJob
    displayName: 'Análise estática de código'
    steps:
    - task: UseDotNet@2
      displayName: 'Instalar .NET SDK 8.x'
      inputs:
        version: '8.x'

    - script: dotnet build --configuration $(buildConfiguration) /p:RunAnalyzers=true /p:TreatWarningsAsErrors=false
      displayName: 'Build com análise de código'

    - script: dotnet format --severity warn
      displayName: 'Verificar formatação do código'

# ─────────────────────────────────────────
# STAGE 4 — DEPLOY
# ─────────────────────────────────────────
- stage: Deploy
  displayName: 'CD - Deploy para Azure'
  dependsOn: Quality
  condition: succeeded()
  jobs:
  - deployment: DeployJob
    displayName: 'Deploy para App Service'
    environment: 'production'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureWebApp@1
            displayName: 'Deploy para Azure App Service'
            inputs:
              azureSubscription: $(azureServiceConnection)
              appType: 'webAppLinux'
              appName: $(webAppName)
              package: '$(Pipeline.Workspace)/drop/$(Build.BuildId).zip'
              runtimeStack: 'DOTNET|8.0'